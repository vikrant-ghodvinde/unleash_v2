const Loader = () => {
  return (
    <div className="fixed top-0 left-0 w-full h-full min-h-screen flex items-center justify-center bg-black">
      <span className="loader"></span>
    </div>
  );
};

export default Loader;
